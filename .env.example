PORT=5174
HOST=127.0.0.1
JUDGE_JURY_DB_PATH=data/judge-jury.sqlite
UPLOAD_TMP_DIR=uploads/tmp
JUDGE_JURY_MAX_UPLOAD_BYTES=262144000

# MiniMax runtime
MODEL_PROVIDER=minimax
MINIMAX_API_KEY=
MINIMAX_MODEL=MiniMax-M3
MINIMAX_BASE_URL=https://api.minimax.io/v1
MODEL_TIMEOUT_MS=60000
MODEL_MAX_RETRIES=2

# Optional local/OpenAI-compatible runtime, for example Ollama.
# Set MODEL_PROVIDER=openai-compatible to use these values.
OPENAI_COMPATIBLE_BASE_URL=http://localhost:11434/v1
OPENAI_COMPATIBLE_API_KEY=ollama
OPENAI_COMPATIBLE_MODEL=qwen2.5:14b

# Set to 1 for deterministic local development without live model calls.
MINIMAX_MOCK=1

# Local structured JSONL logs. Set LOG_ENABLED=0 to disable.
LOG_DIR=logs
LOG_LEVEL=debug
LOG_ENABLED=1
